version: '3.0'
services:
  mapproxy:
    build: src
    ports:
      - "8000:8000"
    environment:
      - UWSGI_HTTP=0.0.0.0:8000
      - UWSGI_MASTER=1
      - UWSGI_PROCESSES=4

  database:
    image: amsterdam/postgres
    environment:
      POSTGRES_PASSWORD: insecure
      POSTGRES_USER: postgres
    extra_hosts:
          admin.datapunt.amsterdam.nl: 10.99.157.24

  mapserver:
    image: build.datapunt.amsterdam.nl:5000/datapunt/mapserver-private
    user: root
    environment:
      ATLAS_DB_HOST: database
      ATLAS_DB_USER: postgres
      BASISKAART_DB_HOST: database
      BASISKAART_DB_USER: postgres
      MAP_URL: "http://mapserver"

  topo_rd:
    build: src
    user: root
    links:
      - mapserver
      - database
    command: >
      mapproxy-seed -c 10 -s /app/seed.yaml -f /app/mapproxy.yaml --seed=topo_rd_kbk,topo_rd_bgt
    volumes:
      - "/mnt/tiles:/mnt/tiles"

  topo_rd_light:
    build: src
    user: root
    links:
      - mapserver
      - database
    command: >
      mapproxy-seed -c 10 -s /app/seed.yaml -f /app/mapproxy.yaml --seed=topo_rd_light_kbk,topo_rd_light_bgt
    volumes:
      - "/mnt/tiles:/mnt/tiles"

  topo_rd_zw:
    build: src
    user: root
    links:
      - mapserver
      - database
    command: >
      mapproxy-seed -c 10 -s /app/seed.yaml -f /app/mapproxy.yaml --seed=topo_rd_zw_kbk,topo_rd_zw_bgt
    volumes:
      - "/mnt/tiles:/mnt/tiles"

  topo_wm:
    build: src
    user: root
    links:
      - mapserver
      - database
    command: >
      mapproxy-seed -c 10 -s /app/seed.yaml -f /app/mapproxy.yaml --seed=topo_wm_kbk,topo_wm_bgt
    volumes:
      - "/mnt/tiles:/mnt/tiles"

  topo_wm_light:
    build: src
    user: root
    links:
      - mapserver
      - database
    command: >
      mapproxy-seed -c 10 -s /app/seed.yaml -f /app/mapproxy.yaml --seed=topo_wm_light_kbk,topo_wm_light_bgt
    volumes:
      - "/mnt/tiles:/mnt/tiles"

  topo_wm_zw:
    build: src
    user: root
    links:
      - mapserver
      - database
    command: >
      mapproxy-seed -c 10 -s /app/seed.yaml -f /app/mapproxy.yaml --seed=topo_wm_zw_kbk,topo_wm_zw_bgt
    volumes:
      - "/mnt/tiles:/mnt/tiles"

  topo-lufo:
    build: src
    user: root
    command: >
      mapproxy-seed -c 6 -s /app/seed.yaml -f /app/mapproxy.yaml --seed=lufo_rd
    volumes:
      - "/mnt/tiles:/mnt/tiles"


